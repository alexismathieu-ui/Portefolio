/**
 * Ancien emplacement des particules CSS — retiré volontairement.
 * Fichier conservé pour éviter des imports / HMR cassés vers un chemin supprimé.
 */
export function HeroFireflies() {
  return null
}
